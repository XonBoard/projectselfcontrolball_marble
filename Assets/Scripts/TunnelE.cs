using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TunnelE : MonoBehaviour
{
    public PhysicMaterial ph1, ph2, ph3, ph4, ph5, ph6;
    private float[] check;
    

    // Update is called once per frame
    void Update()
    {
        if (gameObject.GetComponent<RankManager>().txtRanks[2]) ;
    }

}
